Christos' dotfiles
==================

This system uses stow(8) to deploy the dotfiles.

Usage
-----
        $ cd ~
        $ git clone git://git.margiolis.net/dotfiles.git
        $ cd dotfiles
	# ./autoinstall
